# Как проверить все открытые файлы пользователем или процессом в Linux

Источник: [Как проверить все открытые файлы пользователем или процессом в Linux](https://wiki.it-kb.ru/unix-linux/linux-cli-tools/how-to-check-all-open-files-by-process-or-user-in-linux)

```text
* Лимит ядра Linux

* Методика подсчёта открытых файлов

* Примеры получения данных
```

  * Обсуждение

## Как проверить все открытые файлы пользователем или процессом в Linux

[![](/images/31b4d1658b7c474d94839c61083dc805.png)](/_detail/unix-linux/linux-cli-tools/pasted/20180609-121717.png?id=unix-linux%3Alinux-cli-tools%3Ahow-to-check-all-open-files-by-process-or-user-in-linux "unix-linux:linux-cli-tools:pasted:20180609-121717.png") В некоторых ситуациях на Linux могут возникать ошибки, связанные с превышением лимита использования файловых дескрипторов. Эти лимиты накладываются как самим ядром Linux, так и его программными модулями, например PAM.

### Лимит ядра Linux

Узнать текущее значение максимального количества файловых дескрипторов, определяемое ядром Linux можно командой:

```text
# cat /proc/sys/fs/file-max
```

Этот лимит может быть изменён без перезагрузки системы (начинает действовать сразу и действует до перезагрузки):

```text
# echo "1221724" > /proc/sys/fs/file-max
```

Чтобы требуемое значение использовалось постоянно, то есть действовало и после перезагрузки, его необходимо определить в конфиг.файле `/etc/sysclt.conf`:

[sysclt.conf](/_export/code/unix-linux/linux-cli-tools/how-to-check-all-open-files-by-process-or-user-in-linux?codeblock=0 "Скачать фрагмент кода")

```text
...
fs.file-max=1221724
...
```

### Методика подсчёта открытых файлов

Для получения информации о количестве всех открытых файлов всеми процессами в Linux некоторые «знатоки» предлагают использовать команду типа

```text
# lsof | wc -l
```

Однако такая команда показывает гораздо большее значение, чем всего открыто файлов в системе на данный момент на самом деле. Это связано с тем, что по несколько раз в подсчёт попадают одни и теже открытые файлы, используемые разными процессами.

Поэтому проще для получения общего числа открытых файлов использовать данные ядра Linux

```text
# cat /proc/sys/fs/file-nr
5248    0       610862
```

Первое число - общее количество занятых/используемых на данный момент времени файловых дескрипторов.
Второе число - количество выделенных процессам, но не используемых в данный момент дескрипторов.
Третье число - максимальное количество открытых дескрипторов

### Примеры получения данных

Получить список TOP-20 процессов с самым большим количеством открытых файловых дескрипторов:

```text
# for x in `ps -eF| awk '{ print $2 }'`; \
 do echo `ls /proc/$x/fd 2> /dev/null | \
 wc -l` $x `cat /proc/$x/cmdline 2> /dev/null`; \
 done | sort -n -r | head -n 20
```

Подсчитать количество открытых файлов в разрезе процессов (в первой колонке будет выведен PID процесса, во второй количество открытых файлов этим процессом):

```text
# ps aux | sed 1d | awk \
 '{print "fd_count=$(lsof -p " $2 " | wc -l) && echo " $2 " $fd_count"}' | \
 xargs -I {} bash -c {}
```

Посмотреть открытые файловые дескрипторы во всех процессах для отдельно взятого пользователя, например «apache»

```text
# lsof -u apache
```

Подсчитать количество открытых файлов в каждом процессе для отдельно взятого пользователя:

```text
# lsof -u apache | awk '{print $2}' | sort | uniq -c | sort -n
```

Тоже самое, только в реальном режиме времени:

```text
# watch 'lsof -u vdsm | awk '\''{print $2}'\'' | sort | uniq -c | sort -n'
```

Посмотреть открыте файловые дескриптры для отдельно взятого процесса (по PID процесса):

```text
# lsof -p 9189
```

Подсчитать количество файловых дескриптров для отдельно взятого процесса:

```text
# lsof -p 9189 | wc -l
```

* * *

Дополнительные источники информации:

  * [Блог Дениса Тулякова - Количество открытых файлов, сокетов](https://blog.dtulyakov.ru/2015/02/files-max.html "https://blog.dtulyakov.ru/2015/02/files-max.html")

* * *

Проверено на следующих конфигурациях:

Версия ОС
---
Debian GNU/Linux 8.10 (jessie)

* * *

![](/images/35b7a2b4cdc2c64fa78115def5ebc295.png) Автор первичной редакции:
[Алексей Максимов](/user/blogroot "user:blogroot")
Время публикации: 09.06.2018 11:18

[Linux](/tag/linux?do=showtag&tag=Linux "tag:linux"), [Kernel](/tag/kernel?do=showtag&tag=Kernel "tag:kernel"), [File server](/tag/file_server?do=showtag&tag=File_server "tag:file_server"), [File system](/tag/file_system?do=showtag&tag=File_system "tag:file_system"), [PAM](/tag/pam?do=showtag&tag=PAM "tag:pam"), [Shell](/tag/shell?do=showtag&tag=Shell "tag:shell")

##  Обсуждение

Полное имя:

Эл. адрес:

Ваш комментарий:

Имя друга Чебурашки (при написании вместо буквы "а" используйте @) Пожалуйста, оставьте это поле пустым:

Подписаться на комментарии

unix-linux/linux-cli-tools/how-to-check-all-open-files-by-process-or-user-in-linux.txt · Последнее изменение: 09.06.2018 09:49 — Алексей Максимов

* * *

**********

[файловые дескрипторы](/tags/failovye_deskriptory.md)
